load("@rules_kotlin//kotlin:jvm.bzl", "kt_jvm_library", "kt_jvm_test")
load(":deps.bzl", "MODULE_DEPS", "TEST_DEPS")

# 모듈 경계: 이 모듈 안에서만 의존할 수 있다. 다른 모듈은 <module>-api 타깃만 쓴다.
package(default_visibility = ["//systems/configuration:__subpackages__"])

kt_jvm_library(
    name = "main",
    srcs = glob(["src/main/kotlin/**/*.kt"]),
    javac_opts = "//:javac_options",
    kotlinc_opts = "//:kotlinc_options",
    plugins = ["//:spring_allopen"],
    deps = MODULE_DEPS,
)

kt_jvm_test(
    name = "test",
    srcs = glob(["src/test/kotlin/**/*.kt"]),
    javac_opts = "//:javac_options",
    kotlinc_opts = "//:kotlinc_options",
    test_class = "hs.kr.entrydsm.configuration.application.ConfigurationApplicationModuleTest",
    deps = MODULE_DEPS + TEST_DEPS + [":main"],
)

kt_jvm_test(
    name = "file_document_service_test",
    srcs = glob(["src/test/kotlin/**/*.kt"]),
    javac_opts = "//:javac_options",
    kotlinc_opts = "//:kotlinc_options",
    test_class = "hs.kr.entrydsm.configuration.application.FileDocumentServiceTest",
    deps = MODULE_DEPS + TEST_DEPS + [":main"],
)

kt_jvm_test(
    name = "schedule_service_test",
    srcs = glob(["src/test/kotlin/**/*.kt"]),
    javac_opts = "//:javac_options",
    kotlinc_opts = "//:kotlinc_options",
    test_class = "hs.kr.entrydsm.configuration.application.ScheduleServiceTest",
    deps = MODULE_DEPS + TEST_DEPS + [":main"],
)
